module.exports = {
    // PROCESS SUBMISSIONS
    // Store preApprovalInteractionID?
    // Create a new submission doc
    // Generate a submission ID (increments by 1 each time)
    // Use SharpJS to create bigJPG, bigPNG, smallWebp
    // Store under submissions/artwork, submissions/card, submissions/smallCard on BackBlaze

    // APPROVE SUBMISSIONS
    // Add a card document with the info
    // Move images from /submissions

    // REJECT SUBMISSION
    // Store in rejectedSubmissions
    // Remove from submissions
}